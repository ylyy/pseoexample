# GitLab 推送认证说明

## 🔐 问题
推送时提示需要用户名和密码，但 GitLab 不支持直接使用密码。

## ✅ 解决方案：使用 Personal Access Token

### 方法一：在推送时输入 Token（推荐）

1. **创建 Personal Access Token：**
   - 访问：https://gitlab.com/-/user_settings/personal_access_tokens
   - Token name：填 `pseoexample`（任意名字）
   - Scopes：勾选 `write_repository`
   - 点击 **"Create personal access token"**
   - **复制生成的 Token**（只显示一次，要保存好！）

2. **推送时使用 Token：**
   ```bash
   git push -u origin master
   ```
   - Username：输入你的 GitLab 用户名（`gmalpaicm`）
   - Password：**粘贴刚才复制的 Token**（不是密码！）

### 方法二：在 URL 中嵌入 Token（一次性）

修改远程仓库地址，把 Token 嵌入进去：

```bash
# 先删除旧的远程仓库
git remote remove origin

# 添加带 Token 的地址（替换 YOUR_TOKEN）
git remote add origin https://YOUR_TOKEN@gitlab.com/gmalpaicm/pseoexample.git

# 推送
git push -u origin master
```

### 方法三：使用 SSH（最安全，推荐长期使用）

1. **生成 SSH 密钥：**
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   # 按回车使用默认路径
   # 可以设置密码，也可以不设置
   ```

2. **复制公钥：**
   ```bash
   cat ~/.ssh/id_ed25519.pub
   # 复制输出的内容
   ```

3. **添加到 GitLab：**
   - 访问：https://gitlab.com/-/user_settings/ssh_keys
   - 粘贴公钥
   - 点击 **"Add key"**

4. **修改远程仓库地址为 SSH：**
   ```bash
   git remote set-url origin git@gitlab.com:gmalpaicm/pseoexample.git
   ```

5. **推送：**
   ```bash
   git push -u origin master
   ```

---

## 🚀 快速推送（使用 Token）

如果你已经创建了 Token，直接运行：

```bash
git push -u origin master
```

然后输入：
- Username: `gmalpaicm`
- Password: `你的Token`（不是密码！）
