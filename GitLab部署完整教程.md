# GitLab Pages 部署完整教程（零基础版）

## 🎯 目标
把你的网站部署到 GitLab Pages，让全世界都能访问。

---

## 📋 第一步：创建 GitLab 项目

### 1.1 登录 GitLab
1. 打开浏览器，访问：https://gitlab.com
2. 登录你的账号（如果没有账号，先注册）

### 1.2 创建新项目
1. 点击右上角的 **"+"** 按钮
2. 选择 **"New project/repository"**
3. 选择 **"Create blank project"**（创建空白项目）

### 1.3 填写项目信息
- **Project name（项目名称）：** `pseoexample`（或你喜欢的名字）
- **Visibility Level（可见性）：** 选择 **Public**（公开）或 **Private**（私有）
- **Initialize repository with a README：** ❌ **不要勾选**（我们已经有了）

### 1.4 创建项目
点击 **"Create project"** 按钮

### 1.5 复制仓库地址
创建成功后，你会看到一个页面，上面有仓库地址，类似：
```
https://gitlab.com/your-username/pseoexample.git
```
**复制这个地址，后面要用！**

---

## 📤 第二步：推送代码到 GitLab

### 2.1 打开终端（Terminal）
- **Mac：** 按 `Command + 空格`，输入 "Terminal"，回车
- **Windows：** 按 `Win + R`，输入 "cmd"，回车

### 2.2 进入项目目录
```bash
cd /Users/test/pseoexample
```

### 2.3 配置远程仓库
**把下面的 `your-username` 和 `pseoexample` 替换成你刚才创建的项目信息：**

```bash
# 添加远程仓库（替换成你的实际地址）
git remote add origin https://gitlab.com/your-username/pseoexample.git

# 检查是否配置成功
git remote -v
```

**应该会显示：**
```
origin  https://gitlab.com/your-username/pseoexample.git (fetch)
origin  https://gitlab.com/your-username/pseoexample.git (push)
```

### 2.4 推送到 GitLab
```bash
# 推送到 GitLab（使用 master 分支）
git push -u origin master
```

**如果 GitLab 使用 main 分支（新项目默认），运行：**
```bash
# 先重命名分支
git branch -m master main

# 再推送
git push -u origin main
```

### 2.5 输入账号密码
- 如果提示输入用户名和密码：
  - **用户名：** 你的 GitLab 用户名
  - **密码：** 你的 GitLab 密码（或 Personal Access Token）

**如果提示认证失败，需要创建 Personal Access Token：**
1. 进入 GitLab：Settings → Access Tokens
2. 创建一个 Token，权限选择 `write_repository`
3. 用 Token 作为密码

---

## ⚙️ 第三步：配置 GitLab Pages

### 3.1 进入项目设置
1. 在 GitLab 项目页面，点击左侧菜单的 **"Settings"**
2. 找到并点击 **"Pages"**

### 3.2 查看部署状态
- 如果看到 "Your pages are served under..." 说明部署成功
- 如果显示 "Not deployed yet"，需要等待 CI/CD 完成

### 3.3 等待 CI/CD 完成
1. 点击左侧菜单的 **"CI/CD"** → **"Pipelines"**
2. 你会看到一个正在运行的任务（Pipeline）
3. 等待它变成绿色的 ✅（成功）或红色的 ❌（失败）

**通常需要 2-5 分钟**

---

## 🌐 第四步：访问你的网站

### 4.1 获取网站地址
部署成功后，在 **Settings → Pages** 页面会显示你的网站地址：
```
https://your-username.gitlab.io/pseoexample/
```

### 4.2 访问网站
直接在浏览器打开上面的地址，就能看到你的网站了！

---

## 🔧 第五步：更新网站地址（重要！）

部署成功后，需要更新 `sitemap.xml` 和 `robots.txt` 中的地址。

### 5.1 更新 sitemap.xml
1. 打开 `sitemap.xml` 文件
2. 把所有 `your-username.gitlab.io` 替换成你的实际地址
3. 比如：`https://zhangsan.gitlab.io/pseoexample/`

### 5.2 更新 robots.txt
1. 打开 `robots.txt` 文件
2. 更新 sitemap 地址

### 5.3 重新推送
```bash
git add sitemap.xml robots.txt
git commit -m "Update sitemap and robots.txt with actual domain"
git push
```

---

## ❓ 常见问题

### Q1: 推送时提示 "Permission denied"
**解决：** 检查你的 GitLab 账号权限，或使用 Personal Access Token

### Q2: CI/CD 一直失败
**解决：** 
1. 检查 `.gitlab-ci.yml` 文件是否存在
2. 检查文件路径是否正确
3. 查看 CI/CD 日志，找到错误原因

### Q3: 网站显示 404
**解决：**
1. 确认 CI/CD 已经成功（绿色 ✅）
2. 等待几分钟，GitLab Pages 需要时间生效
3. 检查 `public` 文件夹是否有内容

### Q4: 如何更新网站内容？
**解决：**
```bash
# 修改文件后
git add .
git commit -m "更新内容"
git push
# GitLab 会自动重新部署
```

---

## 📝 快速检查清单

- [ ] 在 GitLab 创建了项目
- [ ] 复制了仓库地址
- [ ] 配置了远程仓库（`git remote add origin`）
- [ ] 推送了代码（`git push`）
- [ ] CI/CD 管道成功（绿色 ✅）
- [ ] 在 Settings → Pages 看到了网站地址
- [ ] 网站可以正常访问
- [ ] 更新了 sitemap.xml 和 robots.txt

---

## 🎉 完成！

如果所有步骤都完成了，你的网站应该已经可以访问了！

**网站地址格式：**
```
https://你的用户名.gitlab.io/项目名/
```

**下次更新内容：**
只需要修改文件，然后 `git add`、`git commit`、`git push` 即可，GitLab 会自动重新部署。
